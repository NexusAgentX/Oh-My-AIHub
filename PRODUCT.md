# 产品说明

> 状态：首个产品方向已确认；API 市场发现与选用设计等待维护者确认

本文档记录 Oh-My-AIHub 已经由维护者确认的稳定产品事实、首版边界和验证标准。尚未实现的内容必须理解为目标要求，而不是当前代码能力；当前实现状态仍以 `ARCHITECTURE.md` 和源码为准。

## 当前设计基线

- 可编辑源文件：`design/sources/core-workflows.op`。
- Git 评审预览：`design/previews/core-workflows.png`。
- 原型覆盖 28 个可点击桌面页面、消费者、共享者、C2C 和管理员四条角色流程、关键异常与恢复状态，以及 1440、900、390 三档响应式规则。
- Feature #17 的原型已由维护者从用户视角确认；Feature #28 新增 API 市场列表、公开渠道详情和加入模型池流程，等待维护者确认后成为最新交互基线。原型不代表功能已经实现，也不替代后续真实用户验证。

## 产品愿景

让受邀的小圈子用户共同提供、聚合和使用分散的 API 中转渠道：知道可靠渠道的人能够安全共享资源并回收预付成本，需要调用模型的人不必自行寻找多个中转站、管理多组凭据或维护本地网关。

平台由两个相互连接的市场构成：

1. **API 渠道市场**：共享者提交自己已经充值的中转站渠道，声明模型、原生协议和价格倍率；消费者用平台 API Key 组合多个渠道并按优先级调用。
2. **积分 C2C 市场**：API 消费产生共享者积分收入；用户通过买卖双向挂单在平台外直接支付人民币，并在平台内原子划转积分。

平台以中心化单实例、中心化零和账本提供账号、代理、计量、冻结、结算和人工仲裁。所谓分散供给只指 API 渠道和信用来源由参与者提供，不代表采用区块链、联邦节点或去中心化账本。

## 目标用户

### 首要用户

- **API 消费者**：希望通过一个平台 API Key 使用多个中转站、多个模型或同一模型的多个来源，又不愿自行部署和维护本地网关的受邀用户。
- **渠道共享者**：已经在中转站注册并充值，愿意提交 Base URL 与 API Key 供其他成员调用，并希望用积分收入平衡上游预付成本的受邀用户。

同一账户可以同时是消费者、共享者、积分买家和积分卖家，不设置固定商家或消费者身份。

### 支持角色

- **管理员**：直接创建和停用账户，维护模型目录与基准价，设置信用额度，处理异常渠道、账本差异和 C2C 争议。

首版不开放自由注册，服务范围保持在管理员认识或明确准入的小圈子。

## 用户问题

### 消费侧

- 中转站数量多，价格、模型覆盖、性能和稳定性良莠不齐，发现与比较成本高。
- 聚合多个中转站或为同一模型配置多个来源时，消费者通常要自己部署网关、保存多组密钥并维护路由逻辑。
- 单次失败、渠道切换、真实费用和历史价格难以在一个地方解释和追踪。

### 供给侧

- 知道可用渠道的人必须先向上游充值，闲置余额和预付成本难以被共同利用。
- 个人缺少安全托管密钥、计量、结算、质量展示和回款的完整工具链。
- 直接共享上游 Key 会暴露凭据，也无法区分不同消费者的用量和费用。

### 小圈子冷启动

- 平台若先发行积分会引入无依据的资产和兑付预期；若所有人都必须先买积分，又会阻塞第一批真实调用。
- API 供给和积分需求互相依赖，需要一种损失可控、能够从零开始的信用与清算方式。

## 产品价值

- 消费者只管理平台 Key 和清晰的“模型 + API 格式”渠道池，不再自行维护多渠道网关。
- 共享者不暴露上游凭据，也能按模型自主定价、观察质量并获得可追踪收入。
- 透明展示价格、性能和稳定性，让用户自己选择便宜、稳定或快速的渠道，而不是依赖模糊总分。
- 以信用额度同时产生正负余额完成冷启动，不预发行积分，并用平衡分录保持全平台账本可解释。
- 通过 C2C 部分成交把共享收入和消费需求连接起来，同时让人民币始终留在交易双方之间。

## 产品原则

1. 平台不统一采购所有渠道，也不承诺任何渠道永久可用。
2. 渠道由用户提供，模型价格由共享者在平台基准价上设置倍率。
3. 积分只用于内部计价和清算；`1 积分 ≈ 1 元人民币` 是参考单位，不是固定兑付承诺。
4. 平台不发行、回购、销毁或做市积分；所有用户与系统账户余额合计始终为零。
5. 每笔余额变化必须有对手方、原因、关联业务和前后状态，不允许直接修改余额。
6. 信用额度只是允许账户临时出现负余额的上限，不是赠送积分，也不限制用户持有正余额。
7. 平台托管积分和上游凭据，不托管、冻结、代收或代付人民币。
8. 请求与响应正文不持久化；调用指标、错误码和原始错误消息持久化。
9. 首版保持原生协议，不进行 Chat Completions、Responses、Anthropic 与 Gemini 之间的格式转换。
10. 渠道 Base URL 不得指向 OpenAI 官方 API 端点，也不得通过重定向到达这些端点。

## 核心领域对象

| 对象 | 定义 |
| --- | --- |
| 账户 | 由管理员创建的统一身份，可同时消费、共享和参与 C2C |
| 模型目录 | 管理员维护的公开模型元数据与四类积分基准价 |
| 渠道 | 共享者提交的中转站 Base URL、加密 Key、状态和运行指标 |
| 渠道模型 | 某渠道支持的模型、单一价格倍率与原生 API 格式集合 |
| 平台 API Key | 消费者创建的访问凭据，每把 Key 独立授权和配置 |
| 模型协议池 | 一把平台 Key 下，以“模型 + API 格式”为键的有序渠道列表 |
| 调用 | 一次消费者请求的整体记录，不包含请求或响应正文 |
| 调用尝试 | 调用对某个渠道的一次实际尝试，记录延迟、结果和原始错误 |
| 积分账户 | 用户账户或平台激励、平台损失等系统账户的账本主体 |
| 交易与分录 | 一次原子业务变化及其平衡记账明细 |
| C2C 挂单 | 用户发布的买单或卖单，可被多个对手方分批承接 |
| C2C 交易单 | 一次部分成交产生的单一买家与单一卖家交易 |

## 产品范围

### 首版范围内

- 管理员直接创建、启停账户并手动设置或调整信用额度。
- 管理员维护模型目录、模型能力和输入、输出、缓存创建、缓存读取四类基准价。
- 共享者安全提交、测试、暂停和维护渠道；选择支持模型、声明原生协议并为每个模型设置一个统一倍率。
- 消费者浏览只包含已发布、可用渠道的 API 市场，按模型与原生协议筛选，分别比较实际价格、可用性和速度，再从公开详情加入模型协议池。
- 消费者创建任意多把平台 API Key，每把 Key 独立配置可访问模型和模型协议池。
- 每个模型协议池按人工优先级顺序访问渠道；输出开始前失败则依次回退。
- 平台代理原生格式请求，记录调用与尝试指标，按最终成功渠道计量和结算。
- 中心化零和账本、信用、冻结、平台激励账户、平台损失账户和可审计人工调整。
- C2C 买卖双向挂单、单笔最小与最大数量、多个独立部分成交、外部付款确认和人工仲裁。
- 管理员查看账本、信用、API、渠道、共享者收入和 C2C 运行指标。
- 由维护者控制的海外中心化实例，通过互联网服务受邀用户。

### 首版范围外

- 公开注册、邀请链接、自助身份认证或固定商家角色。
- OpenAI 官方 API 直连渠道。
- API 格式转换、模型别名自动映射或跨协议回退。
- 权重随机、成本最优、智能质量、请求对冲或其他复杂路由算法。
- 自动抓取模型官方价格、汇率、榜单或渠道余额。
- C2C 价格交叉自动撮合、快捷买积分、单个交易单包含多个对手方、商家保证金或自动仲裁。
- 平台人民币托管、积分发行、销毁、回购、做市或固定人民币兑付。
- 区块链、链上资产、联邦节点、多租户实例或跨实例清算。
- 保存、搜索或分析 API 请求正文与响应正文。
- 向陌生公众开放或承诺大规模生产容量。

## 核心使用流程

### 1. 管理员准备账户与模型目录

1. 管理员创建账户，初始积分余额为零。
2. 管理员按熟悉程度和可承受风险设置信用额度。
3. 管理员维护模型元数据和四类基准价；具体 API 格式由共享者在每个渠道模型配置中声明。
4. 模型或基准价变更只影响新配置与新调用，历史账单保留快照。

### 2. 共享者上架渠道

1. 共享者在外部中转站注册、充值并取得 Base URL 与 API Key。
2. 平台规范化并校验 Base URL，拒绝非 HTTPS、危险网络目标、OpenAI 官方端点及其重定向落点。
3. 平台加密保存 Key；创建后不再向前端或读取接口回显完整值。
4. 共享者从模型目录选择模型，为每个模型设置一个倍率并声明原生 API 格式。
5. 共享者测试并上架渠道，之后可以查看调用、收入和质量指标或暂停渠道。

### 3. 消费者配置平台 API Key

1. 消费者创建平台 Key；完整值只显示一次。
2. 消费者选择这把 Key 可访问的模型与 API 格式。
3. 消费者在 API 市场按模型与 API 格式筛选已发布渠道，分别比较价格、可用性与速度。
4. 消费者从公开渠道详情把原生支持相同组合的启用渠道加入对应模型协议池。
5. 消费者拖动或设置渠道优先级；首版不配置权重。

### 4. 平台代理、回退与结算

1. 平台先校验 Key、模型、协议、权限、余额和信用；失败时不访问上游。
2. 平台从池内最高优先级渠道开始尝试。
3. 在尚未向消费者输出内容时，网络错误、超时或上游非成功响应触发下一渠道。
4. 一旦开始输出响应内容，后续中断不再切换，避免重复或混合输出。
5. 中间失败保存调用尝试、指标、错误码和原始错误消息，不向消费者收费；可能产生的上游成本由共享者承担。
6. 全部失败时返回最后一次失败，不收费。
7. 成功时根据用量、当时的基准价和倍率快照计算实际费用；消费者支付实际费用加手续费，共享者取得完整实际费用，手续费进入平台激励账户。
8. 消费者使用自己的渠道时手续费为零，仍产生同一账户的消费借记和渠道收入贷记两条分录，净余额为零。

### 5. 用户通过 C2C 交易积分

#### 卖单

1. 卖家设置总量、单价和单笔最小、最大成交数量。
2. 发布时冻结全部挂牌积分。
3. 买家选择部分数量后生成独立交易单，该数量从可成交量转为交易中。
4. 买家在外部支付人民币并声明已付款；卖家在支付工具中确认到账后放行。
5. 平台原子划转已冻结积分。取消或超时则恢复到挂单可成交量；取消父挂单只解冻未被交易占用的剩余积分。

#### 买单

1. 买家设置希望购买的总量、单价和单笔最小、最大数量；平台不冻结人民币。
2. 卖家承接部分数量时，平台冻结卖家的对应积分并暂时扣减买单可成交量。
3. 买家付款并由卖家确认后，平台划转积分；取消或超时后恢复买单数量并解冻卖家积分。

付款截图只作为人工争议证据，不能自动释放积分。争议期间积分保持冻结，由管理员决定放给买家、退回卖家、延长核实或限制账户。

## 功能需求

### 账户与权限

- 只有管理员能够创建账户、修改信用额度、维护模型目录和执行仲裁。
- 用户只能管理自己的平台 Key、渠道、挂单与交易，并读取授权范围内的统计信息。
- 停用账户不能发起新调用、上架渠道或创建交易，但历史记录和账本分录必须保留。

### 模型、渠道与定价

- 基准价和实际费用使用确定精度的数值表示，不依赖浮点近似完成结算。
- 同一个渠道模型倍率统一作用于输入、输出、缓存创建和缓存读取价格。
- 每次成功调用必须保存模型、四类基准价、倍率、手续费率、用量和费用快照。
- 价格、性能和稳定性分别展示，至少支持按价格、TTFT、TPS、成功率、最近可用率、错误率、评分和累计调用量理解渠道。
- API 市场只展示已发布且当前可用的渠道模型报价，并明确显示模型、原生协议、共享者、倍率、实际输入输出价格、成功率、TTFT、TPS 和最近测试时间。
- API 市场支持按模型与原生协议筛选，并按实际价格、成功率、TTFT 或 TPS 排序；首版不引入模糊综合评分或自动推荐算法。
- 公开渠道详情不得展示完整 Base URL、上游 API Key 或其他共享者凭据；账户自己的渠道可以标记“我的 · 0 手续费”。
- 共享者负责渠道可用性和上游充值；平台不保证渠道永久可用。

### API 调用与错误

- 调用记录与每次渠道尝试分开保存，以解释回退路径和最终结果。
- 原始错误消息与普通运行指标使用相同的访问和保留规则，不另设脱敏流程。
- 平台自身不得把凭据、请求正文或响应正文拼接到错误消息或运行日志中。
- 只有最终成功渠道触发结算，重复回调或重试不得重复扣款。

### 账本与信用

- 每个账户初始余额为零，信用额度只定义最低允许余额。
- 所有用户、平台激励和平台损失等系统账户余额总和始终为零。
- 每次交易必须原子提交平衡分录，并具有幂等键、业务原因和关联对象。
- 冻结只是同一账户内可用状态的变化，不产生或销毁积分。
- 负余额不得直接删除；需要由平台承担时转入平台损失账户。
- 全局 API 手续费由管理员配置，默认 0.1%；首版 C2C 不收平台手续费。
- 激励账户的积分仍属于账本范围，后续使用也必须通过平衡交易发放。

### C2C

- 买单和卖单都允许多个对手方分批承接，但每个子交易只能包含一个买家和一个卖家。
- 并发承接不能超过父挂单剩余量或重复冻结同一积分。
- “我已付款”只是买家声明，不能触发自动放行。
- 正常放行由实际收款的卖家确认；争议由管理员人工处理。

## 非功能需求

### 一致性与审计

- 余额、冻结、信用校验和业务状态变更必须在同一事务边界内完成。
- 所有记账入口幂等，能够抵抗重复请求、并发竞态和部分失败。
- 账本、调用、尝试、挂单和交易单能够相互追溯，历史价格不可被后续配置覆盖。

### 安全

- 上游 API Key 与平台 API Key 都属于高敏感凭据，必须加密或不可逆保存、最小化读取并支持轮换和撤销。
- 上游 Key 写入后不可回显，不得出现在日志、错误响应、分析事件、设计资产或仓库中。
- Base URL 必须执行 HTTPS、规范化、DNS 与 IP 目标、重定向和出站网络校验，防止服务端请求伪造。
- 已知 OpenAI 官方 API 端点使用可配置禁用清单拦截；域名字符串判断不能替代完整的出站安全控制。
- 管理员与普通用户权限分离，余额调整、凭据操作和仲裁保留审计记录。

### 隐私与数据

- 不持久化 API 请求正文或响应正文。
- 保存计费所需用量、价格快照、延迟、状态、渠道、错误码和原始错误消息。
- 首版不为原始错误消息设置独立脱敏、访问或保留策略；平台代码仍不得主动加入凭据或正文。

### 可用性与可观测性

- 核心操作界面通过层级、分组、控件、数据和状态表达操作路径，只保留必要标签、操作、错误与风险确认，不用段落说明代替交互设计。
- 代理必须明确区分平台预校验失败、上游尝试失败、流式输出后中断和最终失败。
- 后台至少统计：全账户余额总和、正负余额、总信用额度、信用使用率、逾期或不活跃负余额、激励账户余额、API 与 C2C 交易量、C2C 价差、积分集中度、渠道成功率和各共享者收入。
- 全账户余额总和不为零属于必须立即调查的账本异常。
- 容量、延迟和可用性数值目标等待真实试用基线，不在产品发现阶段虚构。

### 部署

- 首版由维护者运营一个通过互联网访问的海外中心化实例。
- 不要求多租户、联邦、自托管分发或跨实例清算。
- 正式小圈子试用前需要可恢复备份、凭据配置、运行观测和故障处理路径。

## 产品验证

首轮主要结果不是页面或代码数量，而是一个真实共享闭环是否成立。以下条件全部满足时，首个方向达到初步 `Validated`：

1. 至少 1 名非管理员共享真实渠道。
2. 至少 3 名真实用户在现有客户端或开发工具中使用平台 API Key 完成调用。
3. 至少 1 把 Key 为同一“模型 + API 格式”配置两个渠道，并真实验证优先级或失败回退。
4. 至少一次其他消费者调用为共享者产生积分收入。
5. 至少完成一笔把共享所得积分换回人民币的 C2C 部分成交。
6. 全过程全账户及系统账户余额合计保持为零，没有无法解释的结算差异。

设计、实现、测试和发布准备全部完成只能称为 `Done`；真实用户闭环证据缺一项时，不称为 `Validated`。

## 证据、假设与未知项

### 已确认事实

- 本文档记录的目标用户、双市场定位、计价、路由、账本、C2C、数据和部署边界均由维护者在 Feature #6 中逐项确认。
- [OpenRouter](https://openrouter.ai/docs/guides/routing/provider-selection)、[LiteLLM](https://github.com/BerriAI/litellm-docs/blob/main/docs/proxy/keys_teams_router_settings.md) 和 [Portkey](https://portkey.ai/docs/product/ai-gateway/fallbacks) 的公开实现证明多上游 Key、有序路由、失败回退和逐次尝试记录具有工程可行性；它们不构成本产品的真实用户需求证据。

### 待验证假设

- 小圈子用户愿意把已充值中转站渠道交给平台托管并共享。
- 聚合配置与自动回退带来的便利足以让消费者迁移现有用法。
- 共享者能够通过实际调用和 C2C 获得有意义的成本回收。
- 双边挂单在早期用户规模下具有足够流动性。
- 管理员手动准入、信用和仲裁在首批规模内可持续。

### 待真实使用回答

- 首批用户最常用的模型、协议、客户端、调用量和并发范围。
- 用户在价格、TTFT、TPS、稳定性和评分之间如何取舍。
- 渠道平均失败率、失败成本、余额耗尽频率和维护负担。
- 信用额度的合适初始范围、补足周期和坏账水平。
- C2C 挂单的常见数量、价差、成交时间和争议率。

## 产品路线

面向用户结果的顺序、证据门槛与退出条件统一维护在 [ROADMAP.md](ROADMAP.md)。首版总交付范围由 [Epic #16](https://github.com/NexusAgentX/Oh-My-AIHub/issues/16) 管理；当前设计补充由 [Feature #28](https://github.com/NexusAgentX/Oh-My-AIHub/issues/28) 跟踪，确认后按 #18、#19、#20、#21 和 #23 分批实现。

## 维护规则

- 目标用户、产品问题、价值、范围、核心流程或验证标准变化时更新本文档。
- 优先级、阶段顺序和证据状态变化时更新 `ROADMAP.md`，具体任务和进度只更新对应 Issue body。
- 当前实现与目标架构必须明确区分；只有代码真实交付后才更新 `ARCHITECTURE.md` 的当前结构。
- 影响账本、身份、凭据、协议或部署边界的长期决定写入 `docs/adr/`。
- 不得把 AI 模拟反馈、技术可行性或维护者判断冒充真实用户验证。
