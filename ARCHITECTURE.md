# 架构说明

> 状态：初始工程基线；产品架构待产品定义后完善

本文档描述仓库当前真实存在的系统结构。产品功能尚未确定，因此这里只记录已实现的工程骨架，不预设未来领域模型。

## 当前结构

```text
浏览器
  │
  ├── 开发环境：http://localhost:5173
  │                 │ /api 代理
  │                 ▼
  │              Go 后端：http://localhost:8080
  │
  └── Compose 环境：http://localhost:3000
                    │
                    ▼
                  Nginx
                    │ /api 代理
                    ▼
                  Go 后端：backend:8080
```

## 组件

| 组件 | 位置 | 当前职责 |
| --- | --- | --- |
| 前端 | `frontend/` | React 单页应用；展示服务健康状态 |
| 后端 | `backend/` | Go HTTP 服务；提供 `/api/health` |
| 开发任务 | `mise.toml` | 固定工具版本并提供安装、开发、测试、构建和运行命令 |
| 容器编排 | `compose.yaml` | 构建并运行前后端，依据后端健康状态启动前端 |
| Web 入口 | `frontend/nginx.conf` | 提供前端静态资源，将 `/api/` 代理至后端 |

## 当前请求链路

1. 浏览器加载 React 应用。
2. 前端请求 `/api/health`。
3. 开发环境由 Vite 将请求代理到本机 Go 服务；Compose 环境由 Nginx 代理到 `backend` 服务。
4. 后端返回服务健康状态。

## 数据与状态

当前没有数据库、消息队列、对象存储或已定义的持久化领域数据。

## 外部依赖

当前没有已经确定的第三方 AI 服务或业务系统集成。

## 架构原则

- 以当前源码和有效配置为事实依据，文档不得超前描述尚未实现的能力。
- 在产品边界明确前保持结构简单，避免提前引入无法证明必要性的组件。
- 影响系统边界、数据模型、部署方式、安全模型或长期维护成本的重要决定，应写入 `docs/adr/`。
- 架构变化必须同步更新本文档；只记录历史背景但不再代表现状的内容保留在 ADR 中。

## 尚待确定

- 产品领域边界与核心模型。
- 身份认证、授权与租户模型。
- 数据存储与生命周期。
- AI 模型及第三方服务的接入方式。
- 部署目标、容量、可用性和可观测性要求。
- 隐私、安全与合规边界。

