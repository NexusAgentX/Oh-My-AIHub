# 架构说明

> 状态：初始工程基线；产品目标边界已确认，业务架构尚未实现

本文档首先描述仓库当前真实存在的系统结构，再单独列出已经通过但尚未实现的目标约束。不得把规划约束当作当前代码能力。

## 当前结构

```text
浏览器
  │
  ├── 开发环境：http://localhost:5173
  │                 │ /api 代理
  │                 ▼
  │              Go 后端：http://localhost:8080
  │
  └── Compose 环境：http://localhost:3000
                    │
                    ▼
                  Nginx
                    │ /api 代理
                    ▼
                  Go 后端：backend:8080
```

## 组件

| 组件 | 位置 | 当前职责 |
| --- | --- | --- |
| 前端 | `frontend/` | React 单页应用；展示服务健康状态 |
| 后端 | `backend/` | Go HTTP 服务；提供 `/api/health` |
| 开发任务 | `mise.toml` | 固定工具版本并提供安装、开发、测试、构建和运行命令 |
| 容器编排 | `compose.yaml` | 构建并运行前后端，依据后端健康状态启动前端 |
| Web 入口 | `frontend/nginx.conf` | 提供前端静态资源，将 `/api/` 代理至后端 |

## 当前请求链路

1. 浏览器加载 React 应用。
2. 前端请求 `/api/health`。
3. 开发环境由 Vite 将请求代理到本机 Go 服务；Compose 环境由 Nginx 代理到 `backend` 服务。
4. 后端返回服务健康状态。

## 数据与状态

当前没有数据库、消息队列、对象存储或已定义的持久化领域数据。

## 外部依赖

当前没有已经确定的第三方 AI 服务或业务系统集成。

## 已确认但未实现的目标边界

- 产品由一个海外中心化实例提供，通过互联网服务管理员直接创建的受邀账户；首版不做联邦、多租户或跨实例清算。
- 平台将代理用户提供的 API 中转渠道，安全托管上游凭据，并按“模型 + API 格式”的有序渠道池执行请求。
- 首版只转发渠道原生支持的 Chat Completions、Responses、Anthropic 或 Gemini 格式，不做协议转换。
- 所有积分、信用、冻结、API 费用、激励和 C2C 划转使用中心化零和复式账本；具体原则见 [ADR-0005](docs/adr/0005-adopt-centralized-zero-sum-ledger.md)。
- 人民币在 C2C 双方之间直接流转，平台只冻结和划转积分。
- 平台不持久化 API 请求正文和响应正文，但保存计费与运行指标、错误码和原始错误消息。
- 渠道 Base URL 属于受控出站入口，需要 HTTPS、SSRF 与重定向防护，并禁止 OpenAI 官方 API 端点。

这些约束将在 Epic #16 的独立 Feature 中设计和交付。只有实现合并并通过相应验收后，才会进入上方“当前结构”。

## 架构原则

- 以当前源码和有效配置为事实依据，文档不得超前描述尚未实现的能力。
- 在产品边界明确前保持结构简单，避免提前引入无法证明必要性的组件。
- 影响系统边界、数据模型、部署方式、安全模型或长期维护成本的重要决定，应写入 `docs/adr/`。
- 架构变化必须同步更新本文档；只记录历史背景但不再代表现状的内容保留在 ADR 中。

## 尚待确定

- 账户认证的具体实现与管理员初始引导方式。
- 事务数据库、迁移工具和密钥管理实现。
- 上游协议客户端、流式代理和用量归一的具体边界。
- 备份、恢复、容量、延迟、可用性和告警的数值目标。
- 首批真实渠道、模型、客户端、并发和调用量基线。
