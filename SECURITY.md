# 安全策略

## 当前支持状态

项目尚未正式发布，目前没有承诺长期安全支持的版本。发现的问题仍应及时处理，避免进入后续版本。

## 报告安全问题

如果发现可能导致凭据泄露、未授权访问、数据暴露、代码执行或供应链风险的问题，请不要在修复前公开漏洞细节。

当前私密报告渠道待项目维护者补充。在渠道确定前，请直接联系仓库维护者，并只提供定位问题所需的最少信息。

报告建议包含：

- 受影响的组件、版本或提交。
- 可复现步骤和必要的环境信息。
- 实际影响与可能的攻击条件。
- 已知的临时缓解措施。
- 联系方式和期望的披露时间。

请勿在报告中附带真实凭据、真实用户数据或与问题无关的敏感信息。

## 处理原则

1. 确认问题是否可复现及其影响范围。
2. 避免在修复前扩散可利用细节。
3. 修复根因，并补充与风险相称的测试。
4. 检查相似入口和可能泄露的凭据或数据。
5. 根据影响更新 `CHANGELOG.md`、架构文档和相关 ADR。
6. 在修复可用后协调披露。

## 开发安全基线

- 不得将令牌、密码、私钥、Cookie、真实用户数据或其他敏感信息提交到仓库。
- 通过环境变量或后续确定的密钥管理机制注入敏感配置。
- 所有外部输入都应在信任边界处验证；错误响应不得泄露内部敏感信息。
- 引入依赖前确认来源和必要性，锁定可重复安装所需的版本信息。
- 涉及认证、授权、数据隔离、文件处理、外部命令或第三方回调的改动，需要专门的安全审查。
- 日志中不得记录凭据或不必要的个人信息。

## 已实现的身份与目录安全边界

- 账户只能由管理员创建；初始密码由密码学安全随机源生成，只在创建成功响应中展示一次，数据库和后续查询不保存或回显明文。
- 密码使用 Argon2id 哈希，当前参数为 64 MiB 内存、3 次迭代、并行度 2；验证器限制可接受参数、Salt 和摘要长度，避免恶意哈希导致异常资源消耗或崩溃。
- 首次登录必须改密；改密验证当前密码、拒绝复用，并在同一数据库事务中提升密码版本、撤销全部旧会话、保存新哈希和创建唯一新会话。
- 会话令牌由密码学安全随机源生成，客户端只通过 Host-only、`HttpOnly`、`SameSite=Lax` Cookie 接收；生产默认 `Secure`。数据库只保存 SHA-256 摘要、密码版本和 24 小时到期时间。只有明确的无效会话或成功撤销才会清除 Cookie；数据库瞬时错误返回服务错误并保留当前会话，避免把基础设施故障伪装成退出成功或登录失效。
- 所有非 GET/HEAD/OPTIONS 请求必须具有与请求协议和 Host 完全一致的 `Origin`；反向代理必须正确传递原始 Host 与 `X-Forwarded-Proto`。
- 所有登录尝试按“规范化用户名 + 客户端 IP”限制为 15 分钟最多 20 次、按客户端 IP 限制为 60 次；其中失败尝试再将两项限制收紧为 8 次和 32 次。组合限流先消费 IP 配额，已受限 IP 不会继续污染用户名组合表。所有改密尝试按账户限制为 8 次、按客户端 IP 限制为 32 次，同样先执行 IP 门禁。单进程为登录和已认证密码操作各保留 2 个 Argon2 工作槽，总并发不超过 4，避免一类密码工作耗尽另一类容量。超长用户名与密码在哈希前拒绝；未知、停用和密码错误账户都走密码验证并返回相同凭据错误。
- 首次改密门禁和管理员授权均由后端路由执行，前端守卫只负责用户体验。停用账户立即失去会话访问；管理员不能停用自己或系统最后一个启用管理员。账户权限、状态、信用额度和模型目录更新都携带服务端版本并使用乐观并发控制，陈旧管理表单返回冲突而不覆盖新状态。
- 管理员账户、信用额度、权限、模型和用户改密进入统一审计表，保存操作者、动作原因、目标、时间与非敏感详情。响应统一禁止缓存，并设置 MIME 嗅探、嵌入和同源 Referrer 防护头；Nginx 也为静态前端设置禁止嵌入与 CSP。
- 积分值与模型价格以九位定点整数保存，以十进制字符串经过 API；不得用浮点数承担最终金额或结算。

本地开发可以显式设置 `COOKIE_SECURE=false`，只用于明文 localhost；面向互联网的部署必须保持默认安全 Cookie，并在可信 HTTPS 反向代理之后运行。Compose 前端只绑定宿主机回环地址，唯一公网入口必须是 TLS 反向代理。安全启动任务要求显式提供前端容器实际观察到的 `TRUSTED_PROXY_CIDR` 和当前 Compose 项目网络的 `BACKEND_TRUSTED_PROXY_CIDRS`；Docker NAT 后这些地址不能按 `127.0.0.1` 或某个固定私网段猜测，部署完成后必须通过 `mise run check-proxy-trust` 验证 HTTPS `Origin` 写入链路。外层代理必须覆盖并传递 `X-Forwarded-Proto` 与 `X-Forwarded-For`，Nginx 只对配置网段采信这些头；Go 后端只对配置的内部代理网段采信 Nginx 的 `X-Forwarded-Proto` 与 `X-Real-IP`。后端不发布宿主机端口，未配置内部可信代理时默认忽略全部转发头。当前限流和 Argon2 门禁是单进程内存状态，不替代生产边缘限流和多实例共享防护。

## 已实现的账本安全边界

- Go 领域层和 PostgreSQL 延迟约束共同验证交易至少包含两条非零平衡分录、账户完整余额链和同步投影；交易封存、分录和持有事件不可修改或删除，纠错只能逐条精确冲正原交易。
- 资产冻结、消费授权、capture/release 与账户投影在同一事务提交。capture 必须唯一关联同一持有、命令、事件和源借记；孤立 capture、伪造冲正、直接投影更新和普通交易使用损失账户都会被数据库拒绝。
- 账本命令以操作命名空间、幂等键和载荷摘要识别；同载荷返回首次结果快照，异载荷冲突，未完成命令不能提交。账户与持有使用稳定锁顺序，避免并发透支、超卖和相同键重复结算。
- 信用冻结使有效信用立即为零，阻止新的借记与持有；账户停用同样阻止新操作。已存在的持有可以完成或释放，避免已经接受的业务永久卡死。超限只由已入账负余额与有效信用比较，不把尚未入账的授权误称为债务。
- 平台激励账户不能出现负余额；平台损失账户只能参与坏账转移及其冲正。坏账只能把既有用户负余额转移给损失账户，不能用来给用户创造正余额。
- 普通账户只能读取本人钱包和分录；管理员才能读取全局指标、其他账户及系统账户，并执行调整或坏账转移。管理员写操作必须保留操作者、原因、业务引用、前后余额和对手方，浏览器不提供通用记账接口。

## 已确认但未实现的产品安全边界

以下要求来自已经确认的首版产品方向，仍由后续 Feature 实现：

- 上游 API Key 和平台 API Key 都按高敏感凭据处理。上游 Key 必须加密保存、写入后不可回显并支持替换与撤销；平台 Key 只在创建时完整显示，持久化时采用不可逆验证或等效安全方案。
- 凭据不得进入日志、错误响应、分析事件、设计源文件、预览、测试夹具或 Git 历史。
- 渠道 Base URL 是服务端请求伪造边界。保存和访问前必须执行协议、规范化域名、DNS 与 IP 目标、端口、重定向和出站网络校验，默认拒绝本机、私有、链路本地和云元数据地址。
- 已知 OpenAI 官方 API 端点使用可配置禁用清单拦截，并在重定向后再次校验；字符串匹配不能替代完整 URL 与网络目标验证。
- API 请求正文和响应正文不持久化。用量、延迟、状态、错误码和原始错误消息可以保存，但平台自身不得把凭据或正文拼入错误。
- 渠道管理、凭据替换和 C2C 仲裁必须有操作者、原因、时间和关联业务审计信息；账本调整与信用变化已经按上方边界记录。
- 账户停用、渠道暂停、Key 撤销和信用冻结必须立即阻止新的受影响操作，同时保留历史审计记录。

首版仍不开放注册和自助找回密码。遗忘凭据后的管理员恢复流程尚未定义，不得用读取旧密码的方式实现。

## 待补充

- 私密漏洞报告渠道。
- 正式版本的安全支持周期。
- 上游凭据密钥管理、API 代理、C2C 和部署方案完成后的跨组件完整威胁模型。
- 备份加密、恢复演练、审计保留和安全告警的具体实现与数值目标。
