# Agent 开发指南

本文件适用于整个仓库，帮助人类与 AI Agent 用一致方式理解项目、管理任务和交付改动。

## 项目与架构

Oh-My-AIHub 目前是产品定义前的全栈工程骨架：

- `frontend/`：React、TypeScript 与 Vite 单页应用。
- `backend/`：Go HTTP 服务。
- `frontend/nginx.conf`：Compose 环境的静态资源入口和 `/api/` 反向代理。
- `compose.yaml`：前后端容器编排。
- `mise.toml`：工具版本及安装、开发、测试、构建、运行任务。

开发环境中，浏览器访问 Vite，`/api` 由 Vite 代理到 Go 后端；Compose 环境中，浏览器访问 Nginx，`/api` 由 Nginx 代理到后端容器。当前只实现 `/api/health` 健康检查，尚未确定产品功能和领域模型。

## 开发指南

首次准备环境：

```bash
mise install
mise run install
```

本地开发分别启动后端和前端：

```bash
mise run dev-backend
mise run dev-frontend
```

使用容器运行：

```bash
mise run up
mise run down
```

先确认改动范围，再选择验证命令。不要把 `mise run test` 当成每次修改后的固定动作；测试策略见“测试、门禁与 CI/CD”。

## 文档路由

开始任务时按需阅读，完成任务时按影响更新：

| 文档 | 用途 | 何时更新 |
| --- | --- | --- |
| `README.md` | 项目入口、环境、命令和目录 | 开发入口、环境要求、命令或目录变化时 |
| `PRODUCT.md` | 产品目标、用户、范围和需求 | 产品方向、流程、范围或验收标准变化时 |
| `DESIGN.md` | 视觉 Token、组件、交互、响应式和无障碍规则 | 视觉语言、Token 或组件设计规则变化时 |
| `ARCHITECTURE.md` | 当前系统结构、边界和数据流 | 组件、接口、数据、部署或技术结构变化时 |
| `SECURITY.md` | 安全边界、基线和漏洞处理 | 威胁、敏感数据、权限或安全要求变化时 |
| `CHANGELOG.md` | 尚未发布及版本化的重要变化 | 对用户、开发或部署有意义的变化发生时 |
| `docs/adr/` | 长期技术决策的背景与取舍 | 出现重要且长期有效的技术决策时 |
| `AGENTS.md` | Agent 工作方式和仓库约束 | 协作流程或开发规则变化时 |

仓库文档必须反映当前事实，应在开发过程中持续更新，不要统一拖到任务最后。

任何视觉、页面或前端组件任务开始前必须阅读 `DESIGN.md`，实现时只能使用其中定义的视觉语言和 Token。若任务需要改变设计规则，应先更新 `DESIGN.md`，重大方向变化还应新增 ADR。

任务的 Spec、Plan、Tasks、Acceptance 不属于仓库文档，禁止在仓库中新建任务说明、实施计划或进度文件；这些内容只写在对应 Issue 的 body 中。

## 基本约束

- 先检查真实源码、有效配置、相关文档和 Git 状态，不得只凭名称或旧描述推断实现。
- 产品尚未定义的内容标记为“待补充”或“待确认”，不得自行发明产品能力。
- 当前项目按未发布中间态处理。除非用户明确要求生产兼容或迁移，否则优先保持代码简洁，允许必要的破坏性调整，不增加无依据的兼容层。
- 改动应聚焦当前 Issue，避免夹带无关重构。
- 不得提交凭据、令牌、私钥、真实用户数据或其他敏感信息。
- 新增依赖前确认必要性，并评估维护、安全与体积成本。
- 长期决策写入 ADR；任务进度、临时方案和实施清单留在 Issue body，不写入 ADR。
- 文档、Issue 和实现出现冲突时，以用户最新明确要求与已验证事实为依据，并在当前任务中同步修正。

## Issue 驱动的开发任务模型

Issue 是一次开发任务的唯一任务文档，也是人类与 AI 共同确认范围、步骤和完成条件的地方。只要任务会修改代码、配置或仓库文档，就应先建立 Issue，再开始实施。

如果用户不了解 Issue，Agent 应主动解释：Issue 可以理解为仓库里的“任务卡片”；用户只需描述目标，Agent 负责判断任务规模、拟好完整正文并协助持续维护，用户不需要先学会 GitHub 的操作细节。

### 选择 Feature 或 Epic

- 小任务：建立一个 `Feature Issue`。它应能作为一个边界清晰、可独立实现和验收的交付单元。
- 大任务：先建立一个 `Epic Issue` 描述总目标，再拆成多个 `Feature Issue`。每个 Feature 独立实现和验收，并在 Epic 的 `Tasks` 中以复选框链接。
- 如果用户没有指定类型，Agent 根据范围、依赖和可验收性判断，并用通俗语言告诉用户为什么这样拆分。
- 不要为了形式把一个简单改动拆成 Epic；也不要把多个依赖复杂、无法一次可靠验收的交付塞进一个 Feature。

### Issue 正文格式

每个 Feature 与 Epic Issue 的 body 必须且只需包含以下四个一级板块：

```markdown
## Spec

## Plan

## Tasks

## Acceptance
```

- `Spec`：写清目标、背景、范围、非目标和约束，消除“要做什么”的歧义。
- `Plan`：写清实现路径、影响区域、顺序、依赖与风险；重大技术取舍应指向 ADR。
- `Tasks`：使用复选框列出可执行步骤。Epic 在这里链接各 Feature Issue。
- `Acceptance`：使用复选框列出可观察、可验证的完成条件，包括必要的代码、文档和验证证据。

任务开始前必须把四个板块写完整。尚未明确到足以填写 Spec 和 Acceptance 时，Agent 应先帮助用户澄清，不能一边猜测一边实施。

### 执行与更新

1. 开工前读取 Issue body、相关仓库文档和 ADR，确认它们与当前源码一致。
2. 开发过程中经常直接编辑 Issue body：计划变化时改 `Plan`，步骤完成时勾选 `Tasks`，取得验收证据时勾选 `Acceptance`。
3. 禁止使用 Issue 评论记录任务计划、进度、验收结果或变更说明；始终更新原 body，使读者只看正文就能知道最新状态。
4. 不得在没有证据时提前勾选。部分完成就保留未完成项，并在对应条目中简要写明当前状态。
5. Feature 进度变化时同步更新所属 Epic 的复选框。Epic 只有在所有 Feature 和总体验收项完成后才能完成。
6. 任务结束前确认所有 `Tasks` 和 `Acceptance` 复选框均已完成，并再次同步受影响的仓库文档。

仓库提供 `.github/ISSUE_TEMPLATE/feature.md` 和 `.github/ISSUE_TEMPLATE/epic.md`。Agent 应优先使用模板创建 Issue，并在开工前用实际内容替换所有提示。

## ADR 决策模型

ADR 记录会长期影响架构的决定，例如系统边界、关键框架、存储、协议、部署方式、身份权限模型或难以撤销的取舍。

- 从 `docs/adr/0000-template.md` 创建新 ADR。
- 使用下一个四位编号，并加入 `docs/adr/README.md` 索引。
- 已通过的 ADR 不改写历史结论；方向改变时新增 ADR，并将旧记录标记为“已取代”。
- 实施步骤和进度不写 ADR，仍由 Issue body 管理。

## 测试、门禁与 CI/CD

验证强度必须与改动风险和影响范围匹配。平时开发遵循“改哪测哪”，发版才执行全量测试和完整门禁。

- 仅修改文档：检查内容、链接和 `git diff --check`；没有代码或配置变化时，不运行代码测试、构建或完整门禁。
- 修改 Go 后端：优先运行受影响包的测试，例如 `go -C backend test ./internal/api`。
- 修改前端：运行与改动相关的测试或类型、构建检查；当前没有更聚焦测试时再使用 `npm --prefix frontend run build`。
- 修改 Compose 或部署配置：运行对应配置检查，例如 `docker compose config --quiet`。
- 跨组件改动、高风险基础设施改动或缺少可靠影响边界时，再扩大验证范围。
- 发版：运行 `mise run test` 及当时定义的全部发布门禁，并记录结果。

禁止为了显得完整而机械执行全量测试。若某项检查与改动无关，应不运行，并在交付说明中明确验证边界。

CI/CD 默认异步看待，不因流程速度较慢而机械等待。只有以下情况需要等待：

- 用户明确要求等待。
- 发版或生产部署的验收依赖结果。
- 合并保护或 Issue 的 Acceptance 明确要求结果。
- 当前风险无法用本地聚焦验证充分覆盖。

不等待时应区分本地验证、Git 状态、CI 状态和部署状态，不得把“已触发”描述成“已通过”或“已部署”。纯文档改动通常无需等待 CI/CD。

## 完成条件

一个 Feature 只有同时满足以下条件才算完成：

- Issue body 中的 `Tasks` 和 `Acceptance` 全部勾选。
- 实现与 Issue Spec 一致，没有混入未声明范围。
- 已执行与风险相称的聚焦验证，并如实记录未执行项。
- 相关仓库文档已随实现更新。
- 需要长期保留的技术决策已写入 ADR。
- Issue body 已反映最终计划、结果和验收状态，且没有用评论代替正文。
